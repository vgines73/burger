<div class="container">
    <div class="row">
        <h1 class="text-center">EAT DEM BURGERS BREH!</h1>
        <div class="d-flex justify-content-center">
            <img src="/assets/img/burger.png" alt="burger">
        </div>

    </div>

    <div class="row">
        <div class="col-md-6">
            <h5 class="text-center">Burgers</h5>
            <ul>
                {{#each burgers}}
                {{#unless devoured}}
                <ul>
                    {{burger_name}} <br>
                    <button class="devour-button btn btn-success" data-id="{{id}}" data-burgerDevoured="{{devoured}}">devour</button>
                </ul>

                {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col-md-6">
            <h5 class="text-center">Devoured "Nom Noms"</h5>
            <ul>
                {{#each burgers}}
                {{#if devoured}}
                <ul>
                {{burger_name}} <br>
                <button class="delete-burger btn btn-success" data-id="{{this.id}}"></button>
                </ul>

                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>

    <div class="row">
        <div class="d-flex justify-content-center">
            <h5>Add a Burger</h5>
        </div>
        <div class="d-flex justify-content-center">
            <div class="card">
                <div class="card-body">
                    <form id="create-form">
                        <div class="form-group">
                            <input type="text" id="burger-name" name="name">
                        </div>
                        <br>
                        <div class="text-center">
                            <button id="submit" type="submit" class="btn btn-primary">Submit</button>
                        </div>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>